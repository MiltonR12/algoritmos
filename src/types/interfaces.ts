export interface Process {
  id: number,
  startProcess: number,
  executionTime: number,
  waitTime: number,
  finishTime: number,
  colorCamp: string,
  fullTime: number,
  index: number,
  priority: number
}